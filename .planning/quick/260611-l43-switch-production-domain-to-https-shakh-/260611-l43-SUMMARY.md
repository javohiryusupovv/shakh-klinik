---
quick_id: 260611-l43
title: Switch production domain to https://shakh-clinics.ru
status: complete
completed: 2026-06-11
duration_minutes: 2
files_changed: 3
commits: 1
---

# Quick Task 260611-l43: Summary

## One-liner

Updated the single SITE_URL source of truth from `shaxklinika.uz` to `shakh-clinics.ru` so that all canonical URLs, Open Graph tags (Telegram/Facebook/WhatsApp previews), robots.txt, and sitemap resolve to the production domain.

## What Was Done

| File | Change |
|------|--------|
| `lib/seo/config.ts` | Fallback on line 4: `?? 'https://shaxklinika.uz'` → `?? 'https://shakh-clinics.ru'` |
| `.env.example` | `NEXT_PUBLIC_SITE_URL=https://shaxklinika.uz` → `NEXT_PUBLIC_SITE_URL=https://shakh-clinics.ru` |
| `.env.local` | Same line updated locally (see note below — not committed) |

## Verification Results

- `grep -rn "shaxklinika.uz" lib/seo .env.local .env.example` — **no matches** (exit 1, clean).
- `grep -n "shakh-clinics.ru" lib/seo/config.ts .env.local .env.example` — **new domain present in all three**.

## Commit

`d163f6a` — `chore(seo): point SITE_URL at production domain shakh-clinics.ru`

Files committed: `lib/seo/config.ts`, `.env.example`

## .env.local Status

`.env.local` **is gitignored** (`.gitignore` line 39). The file was edited locally and now contains `NEXT_PUBLIC_SITE_URL=https://shakh-clinics.ru`, but it was NOT committed to the repository — this is expected behavior.

## Production Deployment Note

The running deployment (Vercel / any hosting provider) must have the environment variable set at the platform level:

```
NEXT_PUBLIC_SITE_URL=https://shakh-clinics.ru
```

Without this, the code-level fallback in `lib/seo/config.ts` will be used, which is now also `https://shakh-clinics.ru` — so the fallback is safe. However, setting it explicitly in the deployment environment is the authoritative production path and ensures the value survives any future fallback changes.

## Scope Boundary Respected

- Contact email `info@shaxklinika.uz` in `app/[locale]/contact/page.tsx` — **not touched**.
- Brand name "ShaxKlinika" / "Shax Klinika" in `messages/*.json` and `app/manifest.ts` — **not touched**.
- These are business decisions explicitly out of scope for this task.

## Deviations

None. Plan executed exactly as written.
